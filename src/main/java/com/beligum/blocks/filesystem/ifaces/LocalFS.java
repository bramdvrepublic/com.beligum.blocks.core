package com.beligum.blocks.filesystem.ifaces;

/**
 * This interface is mainly used to optimize the parsing of media files (when we can skip making a local copy in case the streaming API doesn't work)
 * Created by bram on 2/6/17.
 */
public interface LocalFS
{
}
