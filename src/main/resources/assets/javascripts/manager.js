/**
 * Created by wouter on 19/01/15.
 */
blocks.plugin("blocks.core.Manager", ["blocks.core.Constants", "blocks.core.Overlay", "blocks.core.Broadcaster", function(Constants, Overlay, Broadcaster) {
//    var dragging = false;
//    var active = false;
//
//    // on mouseup && active
//    $(document).on("mouseup.blocks_manager", function(event) {
//        // if block then zoom. If not block and overlay -> unzoom
//        if (!dragging && Broadcaster.block().current != null) {
//            Overlay.overlayForElement(Broadcaster.block().current.element, function(){return true}, function() {
//
//            })
//        }
//
//    });
//
//
//    $(document).on(Broadcaster.EVENTS.START_DRAG, function() {dragging = true;});
//    $(document).on(Broadcaster.EVENTS.END_DRAG, function() {dragging = false;});
//    $(document).on(Broadcaster.EVENTS.ACTIVATE_MOUSE, function() {dragging = false; active = true});
//    $(document).on(Broadcaster.EVENTS.DEACTIVATE_MOUSE, function() {dragging = false; active = false});
//

}]);
